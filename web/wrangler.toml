name = "miwen-guiji"
compatibility_date = "2026-09-23"

# 纯静态资源 Worker：无服务端逻辑，只托管 web/public/ 下的单文件游戏
# 路由：/ → 首页 · /classic/ → 经典版 2.x · /ai/ → AI 拓展版 9.x
[assets]
directory = "./public"
html_handling = "auto-trailing-slash"
not_found_handling = "404-page"
